import { FileSystem, Path } from '@effect/platform';
import { ZKConfiguration } from '@midnight-ntwrk/compact-js/effect';
import { Layer } from 'effect';
/**
 * A {@link ZKConfiguration.ZKConfiguration | ZKConfiguration} implementation that reads ZK assets from the
 * file system.
 *
 * @param baseAssetFolderPath A base path to a folder containing the compiled contract assets.
 *
 * @category layers
 */
export declare const layer: (baseAssetFolderPath?: string) => Layer.Layer<ZKConfiguration.ZKConfiguration, never, Path.Path | FileSystem.FileSystem>;
//# sourceMappingURL=ZKFileConfiguration.d.ts.map